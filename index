
const frame = document.getElementById('excelFrame');
const refreshBtn = document.getElementById('refreshBtn');

refreshBtn.addEventListener('click', () => {
  const src = frame.src;
  frame.src = 'about:blank';
  setTimeout(() => { frame.src = src; }, 120);
});

window.addEventListener('online', () => {
  document.querySelector('.status-wrap span:last-child').textContent = 'Fuente en línea';
  document.querySelector('.status-dot').style.background = '#10a37f';
});

window.addEventListener('offline', () => {
  document.querySelector('.status-wrap span:last-child').textContent = 'Sin conexión';
  document.querySelector('.status-dot').style.background = '#b42318';
});

<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="theme-color" content="#0f0f0f" />
  <title>Radar de Equipos · Cadena</title>
  <meta name="description" content="Radar operativo de estado de equipos a nivel cadena." />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="styles.css" />
</head>
<body>
  <div class="app-shell">
    <header class="topbar">
      <div class="brand">
        <div class="brand-mark" aria-hidden="true">
          <span></span><span></span><span></span><span></span>
        </div>
        <div>
          <div class="eyebrow">LA COMER · RADAR OPERATIVO</div>
          <h1>Estado de Equipos</h1>
        </div>
      </div>
      <div class="status-wrap">
        <span class="status-dot"></span>
        <span>Fuente en línea</span>
      </div>
    </header>

    <main>
      <section class="hero">
        <div class="hero-copy">
          <div class="badge">Cadena · Monitoreo operativo</div>
          <h2>Una vista simple del estado actual de los equipos.</h2>
          <p>
            El tablero se alimenta desde tu archivo de Excel en OneDrive/SharePoint.
            La captura continúa en Forms y Power Automate; esta web sólo presenta el
            resultado en una interfaz más limpia.
          </p>
        </div>
        <div class="hero-meta">
          <div class="meta-card">
            <span class="meta-label">Vista principal</span>
            <strong>Reporte</strong>
          </div>
          <div class="meta-card">
            <span class="meta-label">Actualización</span>
            <strong>Al recargar</strong>
          </div>
        </div>
      </section>

      <section class="panel">
        <div class="panel-head">
          <div>
            <span class="section-kicker">TABLERO EN VIVO</span>
            <h3>Radar de Estado de Equipos</h3>
          </div>
          <div class="panel-actions">
            <button class="ghost-btn" id="refreshBtn" type="button">Actualizar vista</button>
            <a class="primary-btn" href="https://lacomer413-my.sharepoint.com/:x:/g/personal/manuelalvarez_lacomer413_onmicrosoft_com/IQBEkYF2vcneQ7Q8Ofum5R6DAdD9379cuC9-2FtNLEjXSGQ?e=u16GmE" target="_blank" rel="noopener noreferrer">Abrir en Excel</a>
          </div>
        </div>

        <div class="frame-wrap" id="frameWrap">
          <iframe
            id="excelFrame"
            title="Reporte Radar de Equipos"
            src="https://lacomer413-my.sharepoint.com/:x:/g/personal/manuelalvarez_lacomer413_onmicrosoft_com/IQBEkYF2vcneQ7Q8Ofum5R6DAdD9379cuC9-2FtNLEjXSGQ?e=u16GmE&action=embedview&wdAllowInteractivity=True&wdHideGridlines=True&wdHideHeaders=True&wdDownloadButton=False&wdActiveSheet=Reporte"
            loading="lazy"
            allow="clipboard-read; clipboard-write"
            referrerpolicy="strict-origin-when-cross-origin">
          </iframe>
          <div class="frame-note" id="frameNote">
            Si Microsoft solicita inicio de sesión, utiliza tu cuenta de Microsoft 365.
          </div>
        </div>
      </section>

      <section class="info-grid">
        <article class="info-card">
          <span class="info-icon">01</span>
          <h4>Captura</h4>
          <p>Las tiendas responden Microsoft Forms sin entrar al tablero.</p>
        </article>
        <article class="info-card">
          <span class="info-icon">02</span>
          <h4>Consolidación</h4>
          <p>Power Automate y Power Query mantienen la base analítica.</p>
        </article>
        <article class="info-card">
          <span class="info-icon">03</span>
          <h4>Consulta</h4>
          <p>Este sitio funciona como portada web del reporte operativo.</p>
        </article>
      </section>
    </main>

    <footer>
      <span>Radar de Equipos · Cadena</span>
      <span class="footer-muted">Diseño inspirado en una estética editorial minimalista.</span>
    </footer>
  </div>
  <script src="app.js"></script>
</body>
</html>

# Radar de Equipos – Sitio web

Sitio estático listo para publicar en un host gratuito.

## Archivos
- `index.html`
- `styles.css`
- `app.js`

## Fuente de datos
El sitio incrusta la vista web del archivo de Excel ubicado en SharePoint/OneDrive:

`https://lacomer413-my.sharepoint.com/:x:/g/personal/manuelalvarez_lacomer413_onmicrosoft_com/IQBEkYF2vcneQ7Q8Ofum5R6DAdD9379cuC9-2FtNLEjXSGQ?e=u16GmE`

El sitio no copia la base de datos. El flujo operativo sigue siendo:

**Microsoft Forms → Power Automate → Excel/Power Query → Hoja Reporte → Sitio web**

## Publicar gratis

### Netlify
1. Descomprime este ZIP.
2. Entra a Netlify.
3. Arrastra la carpeta completa al área de despliegue manual.
4. Netlify generará una URL HTTPS.

### GitHub Pages
1. Crea un repositorio.
2. Sube `index.html`, `styles.css` y `app.js`.
3. Activa Pages desde Settings → Pages.

### Cloudflare Pages
1. Crea un nuevo proyecto de Pages.
2. Sube o conecta la carpeta/repositorio.
3. No requiere proceso de build; el directorio raíz contiene el sitio.

## Importante sobre SharePoint
La visualización embebida depende de la configuración de permisos de Microsoft 365.

- Si el libro exige autenticación, el usuario verá el inicio de sesión de Microsoft dentro o antes de la vista.
- Si SharePoint impide el iframe por políticas del tenant, usa el botón **Abrir en Excel** o genera desde Microsoft 365 un vínculo específico de **Insertar/Embed** y reemplaza el `src` del iframe en `index.html`.
- Publicar esta web en GitHub Pages/Netlify hace públicos los archivos HTML/CSS/JS. El vínculo de SharePoint queda visible en el código fuente, aunque el contenido del libro seguirá protegido por los permisos configurados en Microsoft 365.

## Personalización
El diseño usa una estética editorial minimalista en blanco, negro y verde, inspirada en el lenguaje visual contemporáneo de OpenAI, sin usar logotipos oficiales.


:root{
  --bg:#f7f7f5;
  --surface:#ffffff;
  --ink:#111111;
  --muted:#6b6b67;
  --line:#deded8;
  --soft:#efefeb;
  --accent:#10a37f;
  --accent-dark:#0b7f65;
  --shadow:0 12px 40px rgba(0,0,0,.06);
}
*{box-sizing:border-box}
html{scroll-behavior:smooth}
body{
  margin:0;
  font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;
  color:var(--ink);
  background:
    radial-gradient(circle at 10% 0%, rgba(16,163,127,.08), transparent 28rem),
    var(--bg);
}
button,a{font:inherit}
.app-shell{max-width:1540px;margin:0 auto;padding:0 28px 36px}
.topbar{
  min-height:88px;
  display:flex;
  align-items:center;
  justify-content:space-between;
  border-bottom:1px solid rgba(17,17,17,.09);
}
.brand{display:flex;gap:14px;align-items:center}
.brand-mark{
  width:38px;height:38px;border-radius:12px;background:var(--ink);
  display:grid;grid-template-columns:1fr 1fr;gap:4px;padding:8px;
}
.brand-mark span{border:2px solid #fff;border-radius:50%}
.eyebrow,.section-kicker,.meta-label{
  font-size:11px;letter-spacing:.14em;text-transform:uppercase;color:var(--muted);font-weight:700
}
h1{font-size:18px;margin:3px 0 0;font-weight:650;letter-spacing:-.02em}
.status-wrap{
  display:flex;align-items:center;gap:8px;
  padding:9px 12px;border:1px solid var(--line);border-radius:999px;background:rgba(255,255,255,.7);
  color:#464641;font-size:13px;font-weight:600
}
.status-dot{width:8px;height:8px;border-radius:50%;background:var(--accent);box-shadow:0 0 0 4px rgba(16,163,127,.12)}
.hero{
  display:grid;grid-template-columns:minmax(0,1.6fr) minmax(280px,.7fr);
  gap:32px;padding:54px 0 36px;align-items:end;
}
.badge{
  display:inline-flex;padding:7px 10px;border-radius:999px;background:#e8f6f1;color:var(--accent-dark);
  font-size:12px;font-weight:700;margin-bottom:16px
}
.hero h2{font-size:clamp(36px,4.6vw,72px);line-height:1.02;letter-spacing:-.055em;margin:0;max-width:930px;font-weight:650}
.hero p{max-width:780px;font-size:17px;line-height:1.7;color:var(--muted);margin:22px 0 0}
.hero-meta{display:grid;grid-template-columns:1fr 1fr;gap:12px}
.meta-card{background:rgba(255,255,255,.72);border:1px solid var(--line);border-radius:18px;padding:18px;backdrop-filter:blur(8px)}
.meta-card strong{display:block;margin-top:8px;font-size:18px}
.panel{
  background:var(--surface);border:1px solid var(--line);border-radius:24px;box-shadow:var(--shadow);
  padding:18px;overflow:hidden
}
.panel-head{
  display:flex;justify-content:space-between;gap:18px;align-items:center;padding:8px 6px 18px
}
.panel h3{font-size:24px;letter-spacing:-.03em;margin:5px 0 0}
.panel-actions{display:flex;gap:10px;flex-wrap:wrap}
.ghost-btn,.primary-btn{
  border-radius:12px;padding:10px 14px;font-weight:700;font-size:13px;cursor:pointer;text-decoration:none;
  transition:.18s ease
}
.ghost-btn{background:#fff;border:1px solid var(--line);color:var(--ink)}
.ghost-btn:hover{background:var(--soft)}
.primary-btn{background:var(--ink);border:1px solid var(--ink);color:#fff}
.primary-btn:hover{transform:translateY(-1px);box-shadow:0 6px 20px rgba(0,0,0,.15)}
.frame-wrap{
  position:relative;border-radius:18px;overflow:hidden;border:1px solid #e5e5df;background:#fafaf8;
}
iframe{width:100%;height:760px;border:0;background:#fff;display:block}
.frame-note{padding:10px 14px;font-size:12px;color:var(--muted);border-top:1px solid var(--line);background:#fafaf8}
.info-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:14px;padding:18px 0}
.info-card{border:1px solid var(--line);border-radius:18px;background:rgba(255,255,255,.66);padding:20px}
.info-icon{display:inline-flex;width:32px;height:32px;align-items:center;justify-content:center;border-radius:9px;background:var(--ink);color:#fff;font-size:11px;font-weight:800}
.info-card h4{font-size:18px;margin:16px 0 7px}
.info-card p{margin:0;color:var(--muted);line-height:1.55;font-size:14px}
footer{
  border-top:1px solid rgba(17,17,17,.09);padding:24px 0 0;
  display:flex;justify-content:space-between;gap:16px;color:#44443f;font-size:12px
}
.footer-muted{color:var(--muted)}
@media(max-width:900px){
  .app-shell{padding:0 16px 24px}
  .hero{grid-template-columns:1fr;padding-top:34px}
  .hero-meta{max-width:520px}
  .panel-head{align-items:flex-start;flex-direction:column}
  iframe{height:680px}
  .info-grid{grid-template-columns:1fr}
}
@media(max-width:560px){
  .status-wrap{display:none}
  .hero h2{font-size:40px}
  .hero-meta{grid-template-columns:1fr}
  iframe{height:600px}
  footer{flex-direction:column}
}
